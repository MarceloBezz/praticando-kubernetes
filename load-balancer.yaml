# LoadBalancer para ambientes de nuvem
apiVersion: v1
kind: Service
metadata:
  name: loadbalancer-vollmed
spec:
  type: LoadBalancer
  selector:
    app: vollmed
  ports:
    - port: 3000
      targetPort: 3000